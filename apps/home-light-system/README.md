# Home Light System

This application manages the home lighting system using the modular home automation platform.

## Structure

- `system-definition`: Declarative configuration of the lighting system.
- `devices`: Device-specific configurations and drivers for lighting hardware.
- `data-transformers`: Logic for transforming sensor data into system state.
