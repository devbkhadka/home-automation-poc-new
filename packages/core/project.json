{
  "name": "@home-automation/core",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/core/src",
  "projectType": "library",
  "tags": ["type:lib"],
  "// targets": "to see all targets run: nx show project @home-automation/core --web",
  "targets": {
    "test": {
      "command": "vitest run",
      "options": {
        "cwd": "packages/core"
      }
    },
    "lint": {}
  }
}
